---
name: miser
description: 在 Codex 中按任务难度和独立性安排 Astra 主代理与不同模型的子代理。用户要求多模型协作、分配子任务或调用 $miser 时使用；普通单步任务不触发。
---

# Miser：Codex 多模型任务分流

用户希望由 GPT-6 Astra 统筹时，让当前 Astra 主代理负责理解目标、确定边界、拆分工作、审查结果和最终交付。Skill 不能改变当前任务的主模型；如果当前不是 Astra，应说明需要用户在模型选择器中切换后开启或继续任务，不要声称已切换。

## 判断是否委派

- 先判断是否存在可独立完成、边界清楚的子任务。只有并行处理能明显节省时间、隔离大量中间信息或增加独立核查价值时，才使用子代理。短任务、紧密串行步骤和会同时修改同一文件的工作由主代理完成。
- 尊重用户指定的模型、代理数量、权限和成本偏好。不要为了凑齐模型而创建子代理，也不要把规划与最终验收完全交给子代理。

## 选模型

| 子任务 | 首选模型 | 典型工作 |
| --- | --- | --- |
| 复杂或含糊的多步执行 | `gpt-6-sol` | 跨文件实现、深入排错、需要工具与验证的工作 |
| 清楚、范围窄、可重复的工作 | `gpt-6-luna` | 定向搜索、资料提取、摘要、简单修改、独立检查 |
| 用户明确指定旧模型，或需要与既有 GPT-5.6 Sol 结果作可比复现 | `gpt-5.6-sol` | 旧工作流复核、回归对比、指定模型的编码任务 |

GPT-5.6 Sol 不作为“中等难度”的固定默认档。若只按难度分配，优先在 GPT-6 Sol 与 GPT-6 Luna 间选择；GPT-5.6 Sol 保留给用户指定或有明确兼容理由的任务。可用时按任务需要设置推理强度，先沿用模型默认值；复杂审查再提高。模型是否可用取决于当前账号、客户端和工作区设置。

## 执行与验收

1. 给每个子代理一个具体目标、输入范围、允许修改的文件或只读要求、预期输出。并行编辑时分配互不重叠的文件或工作区。
2. 创建子代理时显式指定所选模型；如果当前工具不支持指定模型或模型不可用，不要假装已按该模型执行。说明限制，使用可用的代理能力继续完成工作；对用户明确要求必须使用的模型，等待用户决定替代方案。
3. 主代理继续处理自己负责的部分，收齐子代理结果，检查关键事实、修改和必要验证，再整合最终交付。概述实际使用的模型与分工；若发生回退，明确说明。

本 Skill 只指导已有的 Codex 子代理能力。它不安装模型、不修改全局配置，也不授权额外的外部操作。
